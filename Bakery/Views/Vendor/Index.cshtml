@{
  Layout = "_Layout";
}
@using Bakery.Models;

<h1>Here is the Vendor List:</h1>

@if (Model.Count == 0)
{
  <p>No vendors have been added yet!</p>
}
@foreach (Vendor vendor in Model)
{
  <p><a href="/vendors/@vendor.Id/orders">@vendor.VendorName</a></p>
}