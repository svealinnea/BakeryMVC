@{
  Layout = "_Layout";
}
@using Bakery.Models;
<h1>Here are all the orders this Vendor has placed</h1>
<ol>
  @foreach (Order order in @Model["orders"])
  {
    <li><a href='/vendors/@Model["vendor"].Id/orders/@order.Id'>@order.Title</a></li>
  }
</ol>